# facts

本目录存放已确认的客观事实（Fact）。

## 格式

每个事实文件按主题组织，例如：

- `profile.yaml` — 候选人基本信息
- `companies.yaml` — 各公司经历事实
- `projects.yaml` — 各项目事实
- `metrics.yaml` — 量化指标清单

## 原则

- 每条事实必须可核对、可追溯。
- 优先使用 `materials/fine/facts.yaml` 作为统一入口。
- 未来可拆分为多个主题文件。
